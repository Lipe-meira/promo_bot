source_channels: []

telegram_relay:
  catch_up_on_start: true
  catch_up_lookback_hours: 6
  catch_up_max_messages_per_channel: 100
  queue_max_size: 200
  recovery_batch_size: 50
  processing_max_attempts: 5
  processing_stale_after_minutes: 15
  retry_initial_seconds: 2
  retry_max_seconds: 300
  redirect_max_hops: 5
  http_timeout_seconds: 10.0

categories:
  - placas de video
  - processadores
  - SSD
  - memoria RAM
  - monitores
  - celulares

keywords:
  - RTX
  - Ryzen
  - SSD NVMe

blacklist: []
minimum_discount_percent: "10.00"
minimum_score: 50
cooldown_hours: 24
maximum_promotions_per_hour: 10

search_intervals_minutes:
  mercadolivre: 30
  amazon: 60
  shopee: 30
  aliexpress: 60
  kabum: 30

providers:
  mercadolivre:
    enabled: false
    affiliate_mode: disabled
    browser:
      enabled: false
      headless: false
      execution_mode: collect_only
      max_generations_per_hour: 6
      min_interval_seconds: 60
      jitter_seconds: 15
      timeout_seconds: 90
      max_attempts: 3
      circuit_breaker_threshold: 3
      circuit_breaker_cooldown_minutes: 60
      allowed_affiliate_hosts: []
      registered_labels: []
  amazon:
    enabled: false
    affiliate_mode: disabled
  shopee:
    enabled: false
    affiliate_mode: disabled
  aliexpress:
    enabled: false
    affiliate_mode: disabled
  kabum:
    enabled: false
    affiliate_mode: disabled

templates:
  - |-
    🔥 OFERTA ENCONTRADA

    {titulo}
    ✅ Por: {preco_atual}
    💳 Condicao: {condicao_pagamento}

    🛒 {link_afiliado}

    ⏰ Verificado em: {data_hora}
    ⚠️ Preco, estoque e cupom podem mudar a qualquer momento.
    🔗 Link de afiliado: posso receber comissao pela compra.

affiliate_disclosure: "Link de afiliado: posso receber comissao pela compra."
maximum_price: null
allowed_sellers: []
blocked_sellers: []
presentation_timezone: America/Sao_Paulo

provider_rate_limits_per_minute:
  mercadolivre: 20
  amazon: 10
  shopee: 10
  aliexpress: 10
  kabum: 10
